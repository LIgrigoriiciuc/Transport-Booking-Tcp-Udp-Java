# Transport-Booking-Udp-Java
Desktop client-server reservation system for a transport company, built in Java. Uses UDP with application-level reliability (custom ACK and retransmit mechanism). Real-time notifications to all connected clients on reservation changes. TCP version available on the tcp branch.
